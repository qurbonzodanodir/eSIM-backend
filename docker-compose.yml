services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: esim
      POSTGRES_USER: esim
      POSTGRES_PASSWORD: esim
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
